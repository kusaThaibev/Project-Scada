# 📖 คู่มือการใช้งานระบบ SCADA Monitoring System (ฉบับละเอียด)

เอกสารนี้อธิบายเจาะลึกถึงการทำงานของระบบ เพื่อให้คุณสามารถปรับแต่งและดูแลรักษาระบบได้ในระยะยาว

---

## 🏗️ 1. สถาปัตยกรรมระบบ (Architecture)

ระบบถูกแบ่งออกเป็น 2 ส่วนหลักที่ทำงานแยกกันแต่คุยกันผ่าน Database:

1.  **The Engine (Collector)**: เขียนด้วย Python ใช้ระบบ Asynchronous เพื่อดึงข้อมูล 1,000+ Tags พร้อมกันโดยไม่ค้าง
2.  **The Storage (Database)**: ใช้ SQLite/PostgreSQL แบ่งตารางเป็นส่วน "ตั้งค่า" และส่วน "เก็บข้อมูล"

---

## 🗃️ 2. เจาะลึกโครงสร้างตาราง (Database Schema)

### ตาราง `machines`
ใช้เก็บชื่อเครื่องจักรหรือ Line การผลิต เพื่อให้คุณกรองข้อมูลในหน้าเว็บได้ง่าย
*   **ประโยชน์**: เวลาทำ Dashboard คุณสามารถเลือกดู "เฉพาะ Machine นี้" ได้ทันที

### ตาราง `tag_config` (หัวใจของการตั้งค่า)
*   **opc_address**: คือที่อยู่ของ Tag ใน Kepware (ต้องตรงกันเป๊ะ)
*   **deadband**: ค่าความต่างขั้นต่ำ ตัวอย่างเช่น:
    *   ถ้าตั้งไว้ `0.5` และค่าเดิมคือ `20.0`
    *   ถ้าค่าใหม่คือ `20.2` -> ระบบจะ **ไม่อัปเดต** ลงประวัติ (เพื่อประหยัดพื้นที่)
    *   ถ้าค่าใหม่คือ `20.6` -> ระบบจะบันทึกลงประวัติทันที
*   **is_active**: ใช้เปิด/ปิดการดึงข้อมูลโดยไม่ต้องลบ Tag ทิ้ง

### ตาราง `tag_latest_snapshot` (สำหรับการแสดงผล Real-time)
ตารางนี้จะเก็บ **"ค่าล่าสุดตัวเดียว"** ของแต่ละ Tag เสมอ
*   **ทำไมต้องมี?**: เพราะตารางประวัติจะมีข้อมูลเป็นล้านแถว ถ้าหน้าเว็บต้องไปหาค่าล่าสุดจากล้านแถวจะช้ามาก การมีตาราง Snapshot ทำให้หน้าเว็บโหลดเร็วสายฟ้าฟาด (เพราะอ่านแค่ 1,000 แถว)

---

## 📊 3. การจัดการ Tag ผ่าน Excel (CSV)

เพื่อให้ง่ายต่อการจัดการ 1,000+ Tags เราจึงใช้ระบบ CSV Import:

*   **ขั้นตอน**:
    1. แก้ไขไฟล์ในโฟลเดอร์ `Tag/`
    2. รัน `python3 import_csv_tags.py`
*   **เทคนิค**: คุณสามารถแยกไฟล์ได้ เช่น `Line1_Tags.csv`, `Line2_Tags.csv` สคริปต์จะกวาดมาลง DB ให้เองทั้งหมด

---

## 🚀 4. การปรับแต่งเพื่อประสิทธิภาพ (Optimization)

### Bulk Read (การอ่านแบบกลุ่ม)
ใน `main.py` เวอร์ชันล่าสุด ผมได้ใช้เทคนิค **Bulk Read** แทนการวนลูปอ่านทีละตัว
*   **การทำงาน**: โปรแกรมจะรวบรวมรายชื่อ 1,000 Tags แล้วส่งคำขอไปที่ Kepware "ทีเดียว"
*   **ผลลัพธ์**: ลดภาระของ Network และทำให้ข้อมูลทุก Tag มี Timestamp ที่ใกล้เคียงกันมากที่สุด

---

## 🛠️ 5. การแก้ปัญหาที่พบบ่อย (Troubleshooting)

| ปัญหา | สาเหตุที่เป็นไปได้ | วิธีแก้ไข |
| :--- | :--- | :--- |
| **เชื่อมต่อ Kepware ไม่ได้** | IP ผิด หรือไม่ได้เปิด OPC UA ใน Kepware | เช็ค `.env` และ Project Properties ใน Kepware |
| **Status เป็น "Good" แต่ค่าไม่มา** | Address ใน CSV ไม่ตรงกับใน Kepware | ก๊อปปี้ Address จาก Kepware มาวางใน CSV ให้ตรงกัน |
| **โปรแกรมฟ้องว่า Untrusted** | ยังไม่ได้กดยอมรับ Certificate | ไปที่ **OPC UA Configuration** -> **Trusted Clients** แล้วกด **Trust** |
| **Database บวมเร็วเกินไป** | ไม่ได้ตั้งค่า Deadband | ปรับค่า `deadband` ใน CSV ให้เหมาะสมกับค่านั้นๆ |

---

## 🔒 6. ความปลอดภัย

*   **ไฟล์ `.env`**: ห้ามส่งไฟล์นี้ให้คนนอก เพราะมีรหัสผ่าน Database และ Server
*   **Kepware Users**: แนะนำให้สร้าง User เฉพาะสำหรับระบบนี้ใน Kepware และให้สิทธิ์แค่ **Read Only** เพื่อป้องกันการเขียนค่าทับโดยไม่ตั้งใจ
