# --- OPC UA Configuration ---
OPC_SERVER_URL=opc.tcp://127.0.0.1:49320
POLLING_INTERVAL=1.0

# --- Database Configuration ---
# Options: sqlite, postgres
DB_TYPE=sqlite
DB_SQLITE_PATH=/Users/kusa_t/TOA/Project Scada/database/scada_data.db

# PostgreSQL (Only used if DB_TYPE=postgres)
DB_NAME=scada_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# --- Django Configuration ---
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=django-insecure-scada-monitoring-system-key-2026
ALLOWED_HOSTS=localhost,127.0.0.1
